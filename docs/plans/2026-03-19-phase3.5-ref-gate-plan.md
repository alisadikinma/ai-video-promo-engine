> **For Claude:** REQUIRED SKILL: Use gaspol-execute to implement this plan.
> **CRITICAL:** This plan modifies reference documents that form the RAG knowledge base
> for the promo-engine skill. Every edit must preserve existing functionality while adding
> new capabilities. If a section's intent is unclear, STOP and ask — never guess.

## Goal

Add a compulsory Phase 3.5 (Reference Image Validation Gate) to the promo-engine pipeline, replace the single-creator model with a Multi-Character Cast System (max 5 characters with Utama/Pendamping roles), and add institution-aware costume detection. This ensures no image/video generation begins without ALL required reference images validated.

## Architecture Context

From CLAUDE.md:
- **Pipeline:** 5 phases (Brainstorm → Script → Scene Breakdown → Image Prompts → Video Prompts) — becomes 6 with Phase 3.5
- **Reference files:** 22 total (12 storytelling + 7 image-video + 3 global/bridge)
- **Key constraint:** NB2 identity lock supports max 5 characters + 14 objects
- **Current creator model:** Single creator (`creator-profile-system.md`) with optional Ali Sadikin preset
- **Current ref image handling:** `ref/creator-face.png` + `ref/creator-brand.png` — user can SKIP
- **Validation skill:** `promo-validate/SKILL.md` runs 8 checks — needs new checks for cast + Phase 3.5

## Tech Stack

Pure markdown reference documents — no code files. The "test suite" is `/promo-validate` which checks cross-file consistency. All files are in `reference/`, `skills/`, `agents/`, and root `CLAUDE.md`.

## Data Integration Map

| Feature | Data Source | File | Exists? | Action |
|---------|-----------|------|---------|--------|
| Cast config (max_cast, naming) | `global-promo-config.md` Section 7 | `reference/global-promo-config.md` | PARTIAL (has creator, not cast) | **Rewrite** Section 7, add Section 11 |
| Cast builder flow (Phase 1) | `SKILL.md` Step 1.1 | `skills/promo-engine/SKILL.md` | YES (single creator) | **Rewrite** Step 1.1 to cast builder |
| Cast profile template | `creator-profile-system.md` | `reference/creator-profile-system.md` | YES (single creator) | **Major rewrite** → multi-cast |
| Phase 3.5 gate logic | `SKILL.md` (new phase) | `skills/promo-engine/SKILL.md` | NO | **Create** new Phase 3.5 section |
| Ref manifest format | (new output) | `reference/script-to-scene-bridge.md` | NO | **Add** Section 9: Ref Manifest |
| Multi-char NB2 templates | `script-to-scene-bridge.md` Section 3 | `reference/script-to-scene-bridge.md` | YES (single creator) | **Rewrite** for multi-character |
| Multi-char VEO templates | `script-to-scene-bridge.md` Section 4 | `reference/script-to-scene-bridge.md` | YES (single creator) | **Rewrite** for multi-character |
| Institution detection | (new logic) | `reference/creator-profile-system.md` | NO | **Add** new section |
| Ali Sadikin as cast preset | `05-creator-and-holidays.md` | `reference/image-video-gen/05-creator-and-holidays.md` | YES (standalone) | **Update** to work within cast system |
| Agent cast awareness | `agents/promo-engine-agent.md` | `agents/promo-engine-agent.md` | NO | **Add** Phase 3.5 + cast refs |
| New validation checks | `promo-validate/SKILL.md` | `skills/promo-validate/SKILL.md` | YES (8 checks) | **Add** checks 9-11 |
| CLAUDE.md pipeline docs | `CLAUDE.md` | `CLAUDE.md` | YES (5-phase) | **Full update** to 6-phase + cast |

## Phase Structure

---

### Phase A: Global Config — Add Cast & Reference Manifest Settings

**Estimated time:** 5 minutes

**Files:**
- Modify: `reference/global-promo-config.md`

**Steps:**
1. Read `reference/global-promo-config.md` (already read — in context)
2. Rewrite Section 7 (Creator Profile) → Section 7 (Cast System) with multi-character config
3. Add Section 11: Reference Manifest Config (naming conventions, manifest file, ref categories)
4. Add Section 12: Institution Detection Config (known institution mappings)
5. Bump `config_version` to `1.1.0` and update `last_updated`

**Section 7 new content:**

```markdown
## 7. Cast System

| Setting | Value | Notes |
|---------|-------|-------|
| `max_cast` | `5` | Max characters total (matches NB2 identity lock limit) |
| `max_pemeran_utama` | `3` | Max main characters |
| `max_pemeran_pendamping` | `2` | Max supporting characters |
| `cast_profile_file` | `cast-profile.md` | In `{project}/` folder (replaces creator-profile.md) |
| `cast_ref_folder` | `ref/` | Reference images folder |
| `ali_preset_available` | `true` | Ali Sadikin preset can fill 1 cast slot |

### Cast Reference Requirements

| Role | Face Ref | Body Ref | Costume Ref | Identity Lock |
|------|----------|----------|-------------|---------------|
| Pemeran Utama | MANDATORY | MANDATORY | MANDATORY (if institutional) | FULL |
| Pemeran Pendamping | MANDATORY | OPTIONAL | OPTIONAL | PARTIAL |
```

**Section 11 new content:**

```markdown
## 11. Reference Manifest

| Setting | Value | Notes |
|---------|-------|-------|
| `ref_manifest_file` | `ref-manifest.md` | In `{project}/output/` folder |
| `ref_validation_mode` | `hard_block` | Cannot proceed to Phase 4 without 100% refs |

### Naming Conventions

| Category | Pattern | Example |
|----------|---------|---------|
| Cast face | `ref/cast-c{N}-face.png` | `ref/cast-c1-face.png` |
| Cast body | `ref/cast-c{N}-body.png` | `ref/cast-c1-body.png` |
| Cast costume | `ref/cast-c{N}-costume.png` | `ref/cast-c1-costume.png` |
| Product | `ref/product-{name}.png` | `ref/product-hero.png` |
| Environment | `ref/env-{location}.png` | `ref/env-pelabuhan.png` |
| Brand asset | `ref/brand-{asset}.png` | `ref/brand-logo.png` |
| Institutional uniform | `ref/costume-{institution}.png` | `ref/costume-kai.png` |

### Reference Categories (All 5 Hard Block)

| # | Category | Required When |
|---|----------|---------------|
| 1 | Character (cast) | Any scene with character |
| 2 | Product | Any scene showing product |
| 3 | Environment | Any B-Roll or location-specific scene |
| 4 | Brand Assets | Any scene with visible logo/UI/brand |
| 5 | Costume/Uniform | When institution detected |
```

**Section 12 new content:**

```markdown
## 12. Institution Detection

| Institution Keyword | Uniform Type | Example Roles |
|---------------------|-------------|---------------|
| KAI / Kereta Api | Railway uniform | Masinis, petugas stasiun, kondektur |
| Pelindo / Pelabuhan | Port authority uniform | Petugas pelabuhan, operator crane |
| BRI / BNI / BCA / Mandiri | Banking uniform | Teller, CS, manager cabang |
| Pertamina | Energy sector uniform | Operator SPBU, engineer |
| PLN | Electricity utility uniform | Teknisi, petugas meter |
| Telkom / Telkomsel | Telecom uniform | Teknisi, CS |
| Garuda Indonesia | Airline uniform | Pilot, pramugari, ground crew |
| RS / Rumah Sakit | Medical uniform | Dokter, perawat, apoteker |
| TNI / Polri | Military/police uniform | Prajurit, polisi |
| Pos Indonesia | Postal uniform | Petugas pos, kurir |
| Damri | Bus transit uniform | Sopir, kondektur |
| BUMN (generic) | Corporate uniform | Per institution |

**Detection Logic:**
1. Scan brand_name + product description for keywords above
2. If match → AskUserQuestion to confirm: "Terdeteksi produk untuk {institution}. Pakai seragam?"
3. If confirmed → ALL cast in relevant scenes MUST have costume ref
4. If no match → skip costume category (unless user manually adds)
```

**Verification:**
- [ ] Section 7 has `max_cast`, `max_pemeran_utama`, `max_pemeran_pendamping`, `cast_profile_file` settings
- [ ] Section 7 has Cast Reference Requirements table with Utama vs Pendamping differences
- [ ] Section 11 has naming conventions for all 7 ref types
- [ ] Section 11 has 5 reference categories listed
- [ ] Section 12 has at least 12 institution keywords
- [ ] `config_version` updated to `1.1.0`
- [ ] No old `creator_face_file` or `creator_brand_file` settings remain in Section 7

---

### Phase B: Rewrite Creator Profile System → Cast Profile System

**Estimated time:** 10 minutes

**Files:**
- Modify: `reference/creator-profile-system.md`

**Steps:**
1. Read `reference/creator-profile-system.md` (already read — in context)
2. Rewrite document title and Section 1 (Profile Setup Flow) → Cast Builder Flow
3. Rewrite Section 2 (Ali Sadikin Preset) → works as 1 cast slot, not standalone
4. Rewrite Section 3 (Generic Profile Template) → Cast Profile Template with multi-character
5. Rewrite Section 4 (Mandatory Appearances) → Per-role appearance rules
6. Rewrite Section 5 (Product Reference Images) → moved to Phase 3.5 manifest (cross-reference only)
7. Add NEW Section 7: Institution-Aware Costume System
8. Add NEW Section 8: Cast Interaction Templates (multi-character scenes)
9. Keep Section 6 (Holiday Wardrobe Swaps) — now applies per cast member

**Key structural changes:**

**Section 1 new flow (Cast Builder):**
```
Step 1: Cast Size → AskUserQuestion: "Berapa pemeran di video ini?" (1-5)
Step 2: Per-character role → AskUserQuestion: "Pemeran #{N} — Utama atau Pendamping?"
Step 3: Per-character profile → name, gender, age, ethnicity, key_features
Step 4: Institution detection → auto-scan brand, confirm via AskUserQuestion
Step 5: Ali Sadikin preset → can fill 1 Utama slot if selected
```

**Section 3 new template (cast-profile.md):**
```markdown
# Cast Profile — {Video Title}

## Cast Summary
| # | Name | Role | Scenes | Identity Lock | Ref Level |
|---|------|------|--------|---------------|-----------|

## Character {N}: {Name} — {ROLE}
- **Gender / Age / Ethnicity / Key Features / Costume**
- **Reference Phrase** (verbatim for all NB2 prompts involving this character)
- **Ref Files:** ref/cast-c{N}-face.png, ref/cast-c{N}-body.png, ref/cast-c{N}-costume.png
```

**Section 7 (Institution-Aware Costume):**
- Reference `global-promo-config.md` Section 12 for institution keywords
- Costume detection flow with AskUserQuestion confirm
- Per-cast costume assignment

**Section 8 (Cast Interaction Templates):**
- 2-character scene template
- 3+ character scene template
- Character-to-character dialogue template
- Group shot composition rules

**CRITICAL: Remove skip option.** Old Step 3 had "Belum, skip dulu" — this MUST be removed. No skip allowed.

**Verification:**
- [ ] Document supports 1-5 characters
- [ ] Pemeran Utama requires: face + body + costume (if institutional)
- [ ] Pemeran Pendamping requires: face only (body/costume optional)
- [ ] Ali Sadikin preset fills exactly 1 cast slot
- [ ] NO "skip" option anywhere in the document
- [ ] Section 7 has institution detection flow
- [ ] Section 8 has multi-character scene templates
- [ ] All AskUserQuestion blocks have selectable options (never plain text questions)
- [ ] Cast profile template has Reference Phrase per character
- [ ] Naming convention matches `global-promo-config.md` Section 11

---

### Phase C: Update SKILL.md — Cast Builder + Phase 3.5

**Estimated time:** 10 minutes

**Files:**
- Modify: `skills/promo-engine/SKILL.md`

**Steps:**
1. Read `skills/promo-engine/SKILL.md` (already read — in context)
2. Update frontmatter description: "5-phase" → "6-phase" pipeline
3. Update "How to Use This Skill" section: mention "6-phase workflow"
4. Update Reference Files table: add Phase 3.5 entries, rename "Creator profile" → "Cast profile"
5. Update Hard Rules: add rules 15-17 for cast system + Phase 3.5
6. **Rewrite Phase 1 Step 1.1:** Single creator → Cast Builder flow
7. **Add Phase 1 Step 1.1b:** Institution Detection (after product discovery in 1.2)
8. **Add Phase 3.5:** Complete new phase between Phase 3 and Phase 4
9. **Update Phase 4 Step 4.1:** Remove old ref image check (replaced by Phase 3.5 gate)
10. **Update Phase 4 Step 4.2:** NB2 prompts now reference cast-profile.md per character
11. Add Phase 3.5 Quality Gate section
12. Update Phase 5 Step 5.4 Summary: include ref-manifest.md in output files

**Phase 3.5 content to add:**

```markdown
### Phase 3.5: REFERENCE IMAGE COLLECTION (Output: ref-manifest.md)

**Read:** `creator-profile-system.md` (cast profile), `global-promo-config.md` (naming conventions)

**HARD BLOCK:** Cannot proceed to Phase 4 until ALL required references uploaded.

#### Step 3.5.1: Auto-Derive Reference Requirements

Scan `scene-plan.md` and `cast-profile.md` to build reference manifest:

FOR each scene in scene-plan.md:
  IF scene has character → add character ref requirements (per cast role)
  IF scene mentions product → add product ref requirement
  IF scene has unique location → add environment ref requirement
  IF scene shows brand/logo/UI → add brand asset ref requirement
  IF institution detected (from Phase 1) → add costume ref for ALL cast in scene

#### Step 3.5.2: Present Reference Manifest

Present the auto-derived manifest table via AskUserQuestion:

AskUserQuestion:
"Berdasarkan scene plan, kamu butuh {N} reference images.
Upload semua ke folder {project}/ref/ sesuai naming convention.

[manifest table]

Status?"

Options:
A) Sudah semua di-upload, validate
B) Mau adjust manifest (tambah/hapus ref)

NOTE: NO skip option. NO "lanjut dulu tanpa ref". Hard block.

#### Step 3.5.3: Validate File Existence

Check every file in manifest exists in {project}/ref/:
- ALL exist → ✅ PASSED, proceed to Phase 4
- ANY missing → ❌ Show missing list, repeat Step 3.5.2

AskUserQuestion (if missing):
"Masih ada {M} reference image yang belum di-upload:
[missing files list]

Upload dulu, lalu confirm."

Options:
A) Sudah di-upload, validate ulang
B) Mau adjust manifest

#### Step 3.5.4: Save Manifest

Save validated manifest to {output_folder}/ref-manifest.md

AskUserQuestion:
"Reference manifest validated ✅ ({N}/{N} complete). Lanjut ke Phase 4?"

Options:
A) Approve — lanjut ke image prompt generation
B) Mau review manifest lagi
```

**New Hard Rules to add (15-17):**
```
15. **Max 5 cast members** — 1-3 Pemeran Utama + 0-2 Pemeran Pendamping (NB2 identity lock limit)
16. **Phase 3.5 is HARD BLOCK** — cannot proceed to Phase 4 without ALL reference images validated
17. **Cast ref requirements by role** — Utama: face+body+costume(if institutional). Pendamping: face only.
```

**Phase 4 Step 4.1 change:**
Remove the old reference image check (was optional with skip). Replace with:
```
Step 4.1: Load validated ref-manifest.md from Phase 3.5
(All references already validated — no re-check needed)
```

**Verification:**
- [ ] Frontmatter says "6-phase" not "5-phase"
- [ ] Reference Files table has Phase 3.5 row
- [ ] Phase 1 Step 1.1 is cast builder (not single creator)
- [ ] Phase 1 has institution detection step after product discovery
- [ ] Phase 3.5 exists between Phase 3 and Phase 4
- [ ] Phase 3.5 has auto-derive, present, validate, save steps
- [ ] Phase 3.5 has NO skip option
- [ ] Hard Rules include rules 15, 16, 17
- [ ] Phase 4 Step 4.1 no longer has optional ref check
- [ ] Phase 5 Summary includes ref-manifest.md
- [ ] All AskUserQuestion blocks have selectable options

---

### Phase D: Update Script-to-Scene Bridge — Multi-Character Templates

**Estimated time:** 10 minutes

**Files:**
- Modify: `reference/script-to-scene-bridge.md`

**Steps:**
1. Read `reference/script-to-scene-bridge.md` (already read — in context)
2. Update Section 3 (Scene → NB2 Image Prompts): add multi-character prompt templates
3. Update Section 4 (Scene → VEO Video Prompts): add multi-character VEO templates
4. Update Section 7 (Quality Audit Checklist): add cast-related checks
5. Add NEW Section 9: Reference Manifest Generation (auto-derive algorithm)
6. Update Section 8 (Full Production Plan Template): add cast summary and ref-manifest

**Section 3 additions (multi-character NB2):**

Add after existing Frame/Ingredients templates:

```markdown
### For Multi-Character Scenes

When 2+ characters appear in same frame:

**Multi-Character Frame Template:**
SUBJECT: Character {A} ({name}) and Character {B} ({name}) in {interaction}.
Using facial identity from ref/cast-c{A}-face.png for Character {A}.
Using facial identity from ref/cast-c{B}-face.png for Character {B}.
Character {A}: {full description from cast-profile.md}... wearing {costume from ref}.
Character {B}: {full description from cast-profile.md}... wearing {costume from ref}.
INTERACTION: {action between characters from script}.
POSITIONING: {spatial relationship — facing each other, side by side, etc.}
SCENE: {environment from ref/env-{location}.png}.
CAMERA: {shot size to fit both characters} {lens} {aperture}.
LIGHTING: {pattern} {ratio}, {kelvin}K.
TECHNICAL: {aspect_ratio}, {resolution}.

**Character Hierarchy in Frame:**
- Pemeran Utama: Always more prominent (closer to camera, better lit, larger in frame)
- Pemeran Pendamping: Supporting position (slightly behind, beside, or angled)
- Max 3 characters per frame for clean composition
```

**Section 4 additions (multi-character VEO):**

```markdown
### Multi-Character Presenter Scene (Multiple Lip Sync)

~{duration}s, {resolution}, {aspect_ratio}.
Camera: {movement}, {speed}.
Character {A} says: {dialogue — max 8-15 words}.
[pause 0.5s]
Character {B} says: {response — max 8-15 words}.
Character {A}: {micro-reaction during B's dialogue}.
SFX: {effects}.
Ambient: {atmosphere}.
{veo_negative_prompt}
Maintain exact appearance from reference images for both characters.

NOTE: VEO lip sync handles 1 speaker at a time.
For dialogue exchange: intercut or sequential delivery, NOT simultaneous.
```

**Section 9 (NEW — Reference Manifest Generation):**

```markdown
## 9. Reference Manifest Generation (Phase 3.5)

### Auto-Derive Algorithm

INPUT: scene-plan.md + cast-profile.md + strategic-brief.md

FOR each unique character in cast-profile.md:
    IF role == "Pemeran Utama":
        ADD ref/cast-c{N}-face.png (MANDATORY)
        ADD ref/cast-c{N}-body.png (MANDATORY)
        IF institution_detected:
            ADD ref/cast-c{N}-costume.png (MANDATORY)
    IF role == "Pemeran Pendamping":
        ADD ref/cast-c{N}-face.png (MANDATORY)

FOR each scene in scene-plan.md:
    EXTRACT unique locations from visual descriptions
    FOR each unique location:
        ADD ref/env-{location-slug}.png (MANDATORY)

    IF scene mentions product by name:
        ADD ref/product-{product-slug}.png (MANDATORY, deduplicated)

    IF scene mentions brand logo, UI, dashboard:
        ADD ref/brand-{asset-slug}.png (MANDATORY, deduplicated)

IF institution_detected:
    ADD ref/costume-{institution-slug}.png (MANDATORY, shared reference)

OUTPUT: ref-manifest.md with all required files, mapped to scenes, status ⬜
```

**Section 7 (Quality Audit) additions:**

```markdown
- [ ] All cast members' reference phrases used verbatim (not just "creator")
- [ ] Multi-character scenes specify EACH character's identity ref
- [ ] Character hierarchy correct (Utama prominent, Pendamping supporting)
- [ ] Costume matches institution ref (if institutional)
- [ ] ref-manifest.md validated (all files present)
```

**Verification:**
- [ ] Section 3 has multi-character NB2 template with per-character identity refs
- [ ] Section 3 has character hierarchy rule (Utama > Pendamping)
- [ ] Section 4 has multi-character VEO template with sequential dialogue
- [ ] Section 4 notes VEO lip sync is 1 speaker at a time
- [ ] Section 7 has cast-related quality checks
- [ ] Section 8 has cast summary in production plan template
- [ ] Section 9 exists with complete auto-derive algorithm
- [ ] All templates use `cast-c{N}` naming (not old `creator-face.png`)

---

### Phase E: Update Agent — Cast + Phase 3.5 Awareness

**Estimated time:** 5 minutes

**Files:**
- Modify: `agents/promo-engine-agent.md`

**Steps:**
1. Read `agents/promo-engine-agent.md` (already read — in context)
2. Update "YOUR CAPABILITIES" list: add cast system + ref manifest
3. Update "REFERENCE FILES" table: rename creator → cast, add Phase 3.5 refs
4. Update "HARD RULES": add rules 12-14 for cast + Phase 3.5
5. Update "WORKFLOW" section: 5-phase → 6-phase, add Phase 3.5
6. Update "OUTPUT" section: add ref-manifest.md and cast-profile.md

**Specific changes:**

Capabilities list add:
```
8. Multi-character cast management (max 5, Utama/Pendamping roles)
9. Reference image manifest generation and validation (Phase 3.5)
10. Institution-aware costume detection
```

Hard Rules add:
```
12. **Max 5 cast members** — Utama: face+body+costume. Pendamping: face only.
13. **Phase 3.5 HARD BLOCK** — no Phase 4 without validated ref-manifest.md
14. **Institution costume** — auto-detect from brand, confirm with user
```

Workflow update:
```
1. Phase 1: Brainstorm → strategic-brief.md + cast-profile.md
2. Phase 2: Script → av-script.md
3. Phase 3: Scene Breakdown → scene-plan.md
4. Phase 3.5: Reference Collection → ref-manifest.md (HARD BLOCK)
5. Phase 4: Image Prompts → image-prompts.md
6. Phase 5: Video Prompts → video-prompts.md
```

Reference Files table add:
```
| Cast profile | `reference/creator-profile-system.md` (cast system) |
| Ref naming | `reference/global-promo-config.md` Section 11 |
| Institution map | `reference/global-promo-config.md` Section 12 |
```

Output add:
```
- `cast-profile.md`
- `ref-manifest.md`
```

**Verification:**
- [ ] Capabilities mention cast system, ref manifest, institution detection
- [ ] Hard Rules 12-14 present
- [ ] Workflow lists 6 phases including Phase 3.5
- [ ] Reference Files table has cast profile (not "creator profile")
- [ ] Output lists ref-manifest.md and cast-profile.md

---

### Phase F: Update Creator & Holidays — Cast-Aware Preset

**Estimated time:** 5 minutes

**Files:**
- Modify: `reference/image-video-gen/05-creator-and-holidays.md`

**Steps:**
1. Read `reference/image-video-gen/05-creator-and-holidays.md` (already read — in context)
2. Update intro text: Ali Sadikin preset now fills 1 cast slot (Pemeran Utama)
3. Update "Standard Reference Phrase": change `alisadikinface.png` → `ref/cast-c{N}-face.png` (N assigned at cast setup)
4. Update Thumbnail prompt template: use cast ref naming
5. Add note: "When using Ali Sadikin preset, assign to a Pemeran Utama slot in cast-profile.md"
6. Holiday wardrobe swaps now reference cast slot number

**Key change in Standard Reference Phrase:**
```
OLD: Using facial identity from reference image: alisadikinface.png.
NEW: Using facial identity from reference image: ref/cast-c{N}-face.png.
     (where {N} = Ali Sadikin's assigned cast slot number)
```

**Key change in Thumbnail Template:**
```
OLD: FACE REF: alisadikinface.png. [WARDROBE as specified].
NEW: FACE REF: ref/cast-c{N}-face.png. [WARDROBE as specified].
     (N = Ali Sadikin cast slot)
```

**Verification:**
- [ ] Ali Sadikin described as "fills 1 Pemeran Utama cast slot"
- [ ] Reference phrase uses `ref/cast-c{N}-face.png` not `alisadikinface.png`
- [ ] Thumbnail template uses cast ref naming
- [ ] Holiday wardrobe section references cast system
- [ ] No orphaned references to old single-creator model

---

### Phase G: Update Validator — Add Checks 9-11

**Estimated time:** 5 minutes

**Files:**
- Modify: `skills/promo-validate/SKILL.md`

**Steps:**
1. Read `skills/promo-validate/SKILL.md` (already read — in context)
2. Update frontmatter: "8 checks" → "11 checks"
3. Add Check 9: Cast System Consistency
4. Add Check 10: Phase 3.5 Hard Block Enforcement
5. Add Check 11: Ref Naming Convention Consistency
6. Update output format example: 11/11 checks

**Check 9: Cast System Consistency**
```markdown
### Check 9: Cast System Consistency
**Pattern:** References to single-creator model in operational files
**Expected:** All files use cast system terminology (cast-profile, cast-c{N}, Pemeran Utama/Pendamping)
**How to verify:** Search for "creator-profile.md" or "creator-face.png" or "creator-brand.png" in operational files — should return 0 matches (replaced by cast-profile.md, cast-c{N}-face.png)
```

**Check 10: Phase 3.5 Hard Block Enforcement**
```markdown
### Check 10: Phase 3.5 Hard Block Enforcement
**Pattern:** Phase 4 proceeding without Phase 3.5 validation
**Expected:** SKILL.md and agent.md both require Phase 3.5 before Phase 4, with "HARD BLOCK" or "cannot proceed"
**How to verify:** Search SKILL.md and agent.md for Phase 3.5 → must contain "HARD BLOCK" and must appear before Phase 4
```

**Check 11: Ref Naming Convention Consistency**
```markdown
### Check 11: Ref Naming Convention Consistency
**Pattern:** Reference image filenames across all files
**Expected:** All ref image filenames follow `ref/cast-c{N}-{type}.png`, `ref/product-{name}.png`, `ref/env-{loc}.png`, `ref/brand-{asset}.png`, `ref/costume-{inst}.png` patterns
**How to verify:** Search all files for `ref/` filenames — all must match naming patterns from global-promo-config.md Section 11
```

**Verification:**
- [ ] Frontmatter says "11 checks"
- [ ] Checks 9, 10, 11 exist with Pattern/Expected/How to verify
- [ ] Output format example shows 11/11
- [ ] Check 9 catches old creator-profile.md references
- [ ] Check 10 enforces Phase 3.5 hard block

---

### Phase H: Update CLAUDE.md — Full Documentation Sync

**Estimated time:** 10 minutes

**Files:**
- Modify: `CLAUDE.md`

**Steps:**
1. Read `CLAUDE.md` (already read — in context)
2. Update Architecture table: rename `creator-profile-system.md` description → "Cast profile system (multi-character, institution-aware)"
3. Update Reference Files tables: add "Phase 3.5" section, update descriptions
4. Update Key Concepts > Production Pipeline: 5 phases → 6 phases with Phase 3.5
5. Update Key Concepts > Output Modes: add ref-manifest.md to outputs
6. Add NEW section: "Multi-Character Cast System" (after Creator Profile System section)
7. Rewrite "Creator Profile System (Hybrid)" → "Cast System (Multi-Character)"
8. Add NEW section: "Phase 3.5: Reference Image Validation Gate"
9. Add NEW section: "Institution-Aware Costume System"
10. Update "Scene → VEO Mode Mapping" table: add multi-character notes
11. Update NB2 Image Generation section: multi-character prompt formula
12. Update Debugging table: add cast-related debug entries
13. Update Conventions: add "Changing a Cast Setting" section
14. Update Capabilities list: add cast, Phase 3.5, institution detection
15. Update Technical Defaults table: add cast settings
16. Update version to 1.1.0

**Pipeline diagram update:**
```
Phase 1: BRAINSTORM          → Output: strategic-brief.md + cast-profile.md
  ├─ Upload tech doc (optional)
  ├─ Cast builder (1-5 characters, Utama/Pendamping)
  ├─ Institution detection + costume confirmation
  ├─ ...existing steps...
  └─ [USER APPROVAL GATE]

Phase 2: SCRIPT               → Output: av-script.md (unchanged)

Phase 3: SCENE BREAKDOWN       → Output: scene-plan.md (unchanged)

Phase 3.5: REFERENCE COLLECTION → Output: ref-manifest.md  ← NEW
  ├─ Auto-derive from scene-plan.md + cast-profile.md
  ├─ Present manifest checklist
  ├─ User uploads to {project}/ref/
  ├─ Validate ALL refs exist (hard block)
  └─ [HARD BLOCK — 100% required]

Phase 4: IMAGE PROMPTS (NB2)   → Output: image-prompts.md
  ├─ Load validated ref-manifest.md
  ├─ Multi-character NB2 prompts per scene
  ...

Phase 5: VIDEO PROMPTS (VEO)   → Output: video-prompts.md (updated for multi-char)
```

**New Debugging entries:**
```
| Identity conflict between cast | Different characters look similar | Use distinct clothing + accessories + positioning per character |
| Wrong character in scene | Character ref not specified | Each NB2 prompt must reference exact cast-c{N}-face.png |
| Costume doesn't match institution | Wrong/generic uniform | Use ref/costume-{institution}.png as reference, describe details |
| Missing ref blocks Phase 4 | ref-manifest.md validation failed | Upload ALL required refs to {project}/ref/ per manifest |
```

**Verification:**
- [ ] Pipeline shows 6 phases (1, 2, 3, 3.5, 4, 5)
- [ ] Phase 1 output includes cast-profile.md
- [ ] Phase 3.5 fully documented with hard block
- [ ] "Creator Profile System" section replaced with "Cast System"
- [ ] Multi-Character Cast System section exists with max 5 / Utama / Pendamping
- [ ] Institution-Aware Costume section exists
- [ ] Debugging table has 4+ new cast-related entries
- [ ] Capabilities list includes items 8-12 (cast, Phase 3.5, institution, etc.)
- [ ] Technical Defaults has cast settings
- [ ] Reference file count updated (still 22 — no new ref files added, just modified)
- [ ] Version is 1.1.0
- [ ] No old "Creator Profile System (Hybrid)" section remains

---

## Execution Dependencies

```
Phase A (global config) ─────────────┐
                                      ├─→ Phase C (SKILL.md) ──→ Phase E (agent)
Phase B (cast profile system) ───────┤
                                      ├─→ Phase D (bridge)
                                      │
                                      └─→ Phase F (creator holidays)

All A-F complete ──→ Phase G (validator)
All A-G complete ──→ Phase H (CLAUDE.md)
```

**Parallelizable pairs:**
- Phase A + Phase B (independent files)
- Phase C + Phase D + Phase F (after A+B, independent files)
- Phase G + Phase H must be sequential (H is last)

## Final Verification (After All Phases)

After all 8 phases complete, run `/promo-validate` to ensure:
- [ ] All 11 checks pass (including new checks 9-11)
- [ ] No old creator-profile references remain
- [ ] Phase 3.5 hard block enforced in SKILL.md and agent.md
- [ ] Ref naming conventions consistent across all files
- [ ] All cross-references between files are valid
